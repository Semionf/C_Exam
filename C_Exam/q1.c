#include <stdio.h>
#pragma warning (disable:4996)
int main1() {

	int a = 4, b = 7; // instead of ; you should use , to seperate between variables

	float c;

	scanf("%f", &c); //in order to get input of any number you should use & before the var

	if (5 == c) //inside an if sentence in order to check equality you need to write ==

		printf("%f", c); //the function of printing is printf, not print


}